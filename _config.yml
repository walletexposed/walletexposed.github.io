# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.

title: "Wallet.Exposed"
email: "walletexposed@protonmail.com"
description: "Wallet.Exposed is a fraud awareness and prevention service dedicated to preventing scams in the world of cryptocurrency"
baseurl: "" # the subpath of your site, e.g. /blog
url: "https://wallet.exposed" # the base hostname & protocol for your site, e.g. http://example.com
twitter_username: "walletExposed"
github_username:  "walletexposed"

# Google Analytics Settings (Disabled until Privacy Policy is implemented)
google_analytics: false
#google_analytics_id: "UA-189852986-1"

# Build settings
# NOTE: The "theme" variable below is commented out, as its place is taken by the "remote_theme" variable
#theme: minima
remote_theme: "walletexposed/theme.wallet.exposed@main"
plugins:
  - jekyll-feed
  - jekyll-remote-theme

# Exclude from processing.
# The following items will not be processed, by default.
# Any item listed under the `exclude:` key here will be automatically added to
# the internal "default list".
#
# Excluded items can be processed by explicitly listing the directories or
# their entries' file path in the `include:` list.
#
# exclude:
#   - .sass-cache/
#   - .jekyll-cache/
#   - gemfiles/
#   - Gemfile
#   - Gemfile.lock
#   - node_modules/
#   - vendor/bundle/
#   - vendor/cache/
#   - vendor/gems/
#   - vendor/ruby/
